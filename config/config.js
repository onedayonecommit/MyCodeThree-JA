const dotenv = require("dotenv").config();

const config = {
  dev: {
    username: "rudghks09",
    password: "rudghks110",
    host: "localhost",
    database: "mydb",
    dialect: "mysql",
  },
};

module.exports = config;
